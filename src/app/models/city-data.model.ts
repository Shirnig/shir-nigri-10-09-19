import {CityMetadata} from './city-metadata.model';
import {CurrentWeather} from './current-weather.model';

export class CityData {
  cityData: CityMetadata;
  currentWeather: CurrentWeather;
}
